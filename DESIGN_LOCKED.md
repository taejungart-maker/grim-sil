# 🔒 디자인 잠금 안내 (DESIGN LOCKED)

## ⚠️ 중요 공지

**이 프로젝트의 메인 페이지 그리드 디자인은 2025년 12월 25일에 최종 승인되었으며, 현재 잠금 상태입니다.**

## 📋 잠금된 디자인 사양

### 파일 위치
- **파일**: `app/page.tsx`
- **라인**: 270-295번

### 그리드 패턴 (고정)
```typescript
// 0번째 작품: 2x2 크기 (왼쪽 상단 중심)
gridStyle = { gridColumn: "span 2", gridRow: "span 2" };

// 5번째 작품: 1x2 세로형
gridStyle = { gridRow: "span 2" };

// 6번째 작품: 2x1 가로형
gridStyle = { gridColumn: "span 2" };

// 7번째 작품: 1x2 세로형
gridStyle = { gridRow: "span 2" };

// 나머지: 1x1 기본 크기
```

### 시각적 레이아웃
```
┌─────────┬───┬───┐
│         │ 1 │ 2 │
│    0    ├───┼───┤
│  (2x2)  │ 3 │ 4 │
├─────────┼───┴───┤
│ 5  │ 6  (2x1) │
│(1x2)├──────┬───┤
│    │  7  │ 8 │
└────┤ (1x2)├───┤
     │     │ 9 │
     └─────┴───┘
```

## 🚫 변경 금지 사항

### 절대 변경하지 말아야 할 것들:
1. **그리드 패턴 로직** (`app/page.tsx` 270-295번 라인)
   - `gridStyle` 할당 조건문
   - `index === 0`, `index === 5`, `index === 6`, `index === 7` 분기
   - `gridColumn`, `gridRow` 값

2. **그리드 컨테이너 설정** (`app/page.tsx` 263-268번 라인)
   - `gridTemplateColumns`
   - `gridAutoRows`
   - `gap` 값

## ✅ 변경 가능한 사항

다음은 디자인에 영향을 주지 않으므로 안전하게 변경 가능합니다:
- 작품 데이터 (제목, 연도, 이미지 등)
- 색상 테마 (화이트/블랙)
- 작가 정보
- 헤더/푸터 내용

## 🔄 디자인 변경이 필요한 경우

디자인 변경이 반드시 필요한 경우:

1. **사용자 승인 필수**
   - 디자인 변경 전 반드시 사용자에게 확인 요청
   - 스크린샷/목업으로 변경 사항 시연
   - 명시적 승인 후에만 진행

2. **백업 생성**
   ```bash
   # 변경 전 현재 상태 백업
   git checkout -b backup-dec25-design
   git push origin backup-dec25-design
   ```

3. **변경 후 검증**
   - 로컬에서 충분히 테스트
   - 사용자 최종 확인
   - Vercel 배포 전 한 번 더 확인

## 📸 승인된 디자인 참조

현재 승인된 디자인 이미지:
- `desired_design.png` (프로젝트 루트)
- Walkthrough 문서의 스크린샷 참조

## 📞 문의

디자인 관련 질문이나 변경 요청:
- 이슈 생성 시 `[DESIGN CHANGE REQUEST]` 태그 사용
- 사용자 승인 없이 임의 변경 금지

---

**마지막 업데이트**: 2026-01-03 17:05:00 KST  
**디자인 승인일**: 2025-12-25  
**현재 상태**: 🔒 잠금 (LOCKED)
